faster-whisper==1.2.1
pyannote.audio==4.0.7
soundfile==0.14.0
huggingface_hub==1.30.0

# Windows + GPU only — ctranslate2 (faster-whisper's backend) needs CUDA 12
# cuBLAS/cuDNN and won't accept the CUDA 13 build that recent torch ships.
# Skip these two on macOS/Linux or CPU-only setups.
nvidia-cublas-cu12==12.9.2.10
nvidia-cudnn-cu12==9.25.1.1
